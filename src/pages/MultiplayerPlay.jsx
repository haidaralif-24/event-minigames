import { useEffect, useState } from 'react';
import { useRoom } from '../hooks/useRoom.js';
import { submitRapidAnswer, markPlayerDisconnected } from '../services/roomService.js';
import { RAPID_QUESTIONS, getActivePlayerId } from '../services/gameLogic.js';
import { TOKEN_COLORS, ACTIVE_META } from '../data/constants.js';

export default function MultiplayerPlay(){const {room,loading,error,session}=useRoom();const [answer,setAnswer]=useState('');const [busy,setBusy]=useState(false);const [message,setMessage]=useState('');useEffect(()=>()=>{if(session?.roomCode&&session?.playerId)markPlayerDisconnected(session.roomCode,session.playerId).catch(()=>{});},[session?.roomCode,session?.playerId]);if(loading)return <div className="min-h-screen grid place-items-center bg-[#fff8e7]">Connecting…</div>;if(error||!room)return <div className="min-h-screen grid place-items-center bg-[#fff8e7] text-red-600">Game unavailable.</div>;const players=room.players||{};const me=players[session.playerId];const activeId=getActivePlayerId(room);const question=RAPID_QUESTIONS[room.rapidShot?.questionIndex||0];const submitted=Boolean(room.rapidShot?.submitted?.[session.playerId]);const send=async()=>{if(!answer.trim()||submitted)return;setBusy(true);try{await submitRapidAnswer(session.roomCode,session.playerId,answer);setAnswer('');setMessage('Answer locked.');}catch(e){setMessage(e.message||'Could not submit.');}finally{setBusy(false);}};return <div className="min-h-screen bg-[#fff8e7] p-4 text-[#18233f]"><header className="mx-auto mb-5 max-w-2xl text-center"><p className="text-xs font-black uppercase tracking-[.25em] text-[#ff8c4d]">{ACTIVE_META.title}</p><h1 className="font-display text-4xl">{me?.name||session.name}</h1><p className="font-black text-[#4d79ff]">SINGLE LOBBY</p></header><main className="mx-auto max-w-2xl space-y-4"><div className="rounded-3xl border-4 border-[#18233f] bg-white p-5"><div className="flex items-center justify-between"><h2 className="text-xl font-black">Players</h2><b>{Object.values(players).filter(p=>p.connected!==false).length}/7</b></div>{Object.values(players).filter(p=>p.connected!==false).sort((a,b)=>a.id.localeCompare(b.id)).map((p,i)=><div key={p.id} className="mt-2 flex justify-between rounded-xl bg-slate-50 p-3"><b style={{color:TOKEN_COLORS[i%TOKEN_COLORS.length]}}>{p.name}</b><span className="text-sm font-bold text-slate-400">{p.id===activeId?'YOUR TURN':'READY'}</span></div>)}</div>{room.phase==='lobby'&&<div className="rounded-3xl border-4 border-[#18233f] bg-white p-8 text-center"><div className="text-5xl">⏳</div><h2 className="mt-3 text-2xl font-black">Waiting for host</h2><p className="mt-2 font-bold text-slate-500">All seven players should be logged in before the host starts.</p></div>}{room.phase==='rapid-shot'&&<div className="rounded-3xl border-4 border-[#18233f] bg-white p-7"><p className="text-xs font-black uppercase tracking-widest text-[#ff8c4d]">RAPID SHOT {room.rapidShot.questionIndex+1}/3</p><h2 className="mt-3 text-2xl font-black md:text-3xl">{question?.text}</h2><input disabled={submitted||busy} value={answer} onChange={e=>setAnswer(e.target.value)} onKeyDown={e=>e.key==='Enter'&&send()} placeholder="Type your answer" className="mt-6 w-full rounded-xl border-4 border-[#18233f] p-4 font-bold"/><button disabled={submitted||busy||!answer.trim()} onClick={send} className="mt-3 w-full rounded-xl border-4 border-[#18233f] bg-[#4dff79] p-4 font-black disabled:opacity-40">{submitted?'ANSWER LOCKED':busy?'SUBMITTING…':'SUBMIT ANSWER'}</button>{message&&<p className="mt-3 text-center font-bold text-[#4d79ff]">{message}</p>}</div>}{room.phase==='order-reveal'&&<div className="rounded-3xl border-4 border-[#18233f] bg-white p-7"><h2 className="text-3xl font-black">Starting Order</h2>{room.turnOrder.map((id,i)=><div key={id} className={`mt-2 flex justify-between rounded-xl p-3 ${id===session.playerId?'bg-[#4dff79]/30':''}`}><span>#{i+1} {players[id]?.name}</span><b>{room.rapidShot?.scores?.[id]||0}</b></div>)}</div>}{room.phase==='board'&&<div className="rounded-3xl border-4 border-[#18233f] bg-white p-8 text-center"><h2 className="text-3xl font-black">{players[activeId]?.name}'s turn</h2>{activeId===session.playerId?<p className="mt-3 font-black text-[#ff8c4d]">IT'S YOUR TURN — WATCH THE HOST SCREEN</p>:<p className="mt-3 font-bold text-slate-500">Watch the projected board.</p>}</div>}{room.phase==='minigame'&&<div className="rounded-3xl border-4 border-[#18233f] bg-white p-8 text-center"><p className="text-xs font-black uppercase text-[#ff8c4d]">END OF ROUND</p><h2 className="mt-2 text-3xl font-black">{room.minigame?.label}</h2><p className="mt-3 font-bold text-slate-500">{room.minigame?.description}</p>{Object.entries(room.minigame?.results||{}).sort((a,b)=>a[1]-b[1]).map(([id,rank])=><div key={id} className="mt-2 flex justify-between rounded-xl bg-slate-50 p-3"><span>#{rank} {players[id]?.name}</span></div>)}</div>}{room.phase==='finished'&&<div className="rounded-3xl border-4 border-[#18233f] bg-white p-8 text-center"><div className="text-6xl">🏆</div><h2 className="mt-3 text-3xl font-black">{players[room.winner]?.name} wins!</h2></div>}</main></div>}
