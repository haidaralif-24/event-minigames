import { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { ACTIVE_META } from '../data/constants.js';
import { ensureAnonymousAuth } from '../firebase.js';
import { createRoom, joinRoom } from '../services/roomService.js';
export default function MultiplayerLobby2(){const [mode,setMode]=useState('join');const [roomCode,setRoomCode]=useState('');const [name,setName]=useState('');const [pin,setPin]=useState('');const [busy,setBusy]=useState(false);const [error,setError]=useState('');const navigate=useNavigate();const submit=async()=>{setBusy(true);setError('');try{await ensureAnonymousAuth();if(mode==='host'){await createRoom(name||'Host');navigate('/host');}else{await joinRoom(roomCode,name,pin);navigate('/play');}}catch(e){setError(e.message||'Could not connect.');}finally{setBusy(false);}};return <div className="min-h-screen bg-[#fff8e7] flex items-center justify-center p-4"><div className="w-full max-w-lg rounded-[2rem] border-4 border-[#18233f] bg-white p-7 shadow-2xl"><div className="text-center"><div className="text-5xl">🎲</div><p className="mt-3 text-xs font-black uppercase tracking-[.25em] text-[#ff8c4d]">{ACTIVE_META.title}</p><h1 className="font-display text-4xl text-[#18233f]">Multiplayer Lobby</h1><p className="mt-2 text-sm font-bold text-[#687187]">Host one room and connect up to seven players.</p></div><div className="mt-6 grid grid-cols-2 gap-2 rounded-2xl bg-[#18233f] p-1"><button onClick={()=>setMode('join')} className={`rounded-xl py-3 font-black ${mode==='join'?'bg-[#4dff79] text-[#18233f]':'text-white'}`}>JOIN ROOM</button><button onClick={()=>setMode('host')} className={`rounded-xl py-3 font-black ${mode==='host'?'bg-[#4d79ff] text-white':'text-white'}`}>HOST GAME</button></div><div className="mt-5 space-y-3"><input value={name} onChange={e=>setName(e.target.value)} placeholder="Username" maxLength={24} className="w-full rounded-xl border-4 border-[#18233f] px-4 py-3 font-bold outline-none"/>{mode==='join'&&<><input value={roomCode} onChange={e=>setRoomCode(e.target.value.toUpperCase())} placeholder="Room code" maxLength={6} className="w-full rounded-xl border-4 border-[#18233f] px-4 py-3 text-center font-black tracking-[.3em] outline-none"/><input value={pin} onChange={e=>setPin(e.target.value)} placeholder="Player password" type="password" className="w-full rounded-xl border-4 border-[#18233f] px-4 py-3 font-bold outline-none"/></>}{error&&<div className="rounded-xl border-2 border-red-400 bg-red-50 p-3 text-sm font-bold text-red-700">{error}</div>}<button disabled={busy||!name.trim()||(mode==='join'&&(!roomCode.trim()||!pin))} onClick={submit} className="w-full rounded-xl border-4 border-[#18233f] bg-[#4dff79] py-4 text-lg font-black text-[#18233f] shadow-md disabled:opacity-40">{busy?'CONNECTING…':mode==='join'?'JOIN GAME':'CREATE ROOM'}</button></div><p className="mt-5 text-center text-xs font-bold text-[#687187]">Seven players maximum • Passwords are hashed before storage.</p></div></div>}
